{
  "name": "app",
  "version": "1.0.50",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/server.ts",
    "migrate": "bun run ./src/migrate.ts latest && bun run kysely-codegen"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.0.2",
    "@elysiajs/jwt": "^1.0.2",
    "@elysiajs/swagger": "^1.0.3",
    "@types/pg": "^8.11.5",
    "elysia": "latest",
    "kysely": "^0.27.3",
    "kysely-codegen": "^0.14.2",
    "kysely-migration-cli": "^0.4.0",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}